# Claude Code Adapter

This repository packages the `focusgroup` skill. When the user asks to test a conversational
agent or run a persona-driven dynamic evaluation, load `SKILL.md` and follow its selected path.
For repository maintenance, follow `AGENTS.md` instead.
